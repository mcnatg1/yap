{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "live-overlay",
  "description": "Capability for the live overlay window",
  "windows": ["live-overlay"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
